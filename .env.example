# Auth0 client_credentials app used by pnw_api.py to authenticate to the API.
# Copy this file to .env and fill in the values (or export them in your shell).
# Do NOT commit the real .env — it is gitignored.

AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
